@model CommunityEvents.Models.Participant
@{
    ViewData["Title"] = "Add Participant";
}
<div class="page-container">
    <div style="max-width:600px; margin:0 auto;">
        <div style="margin-bottom:24px;"><a class="btn btn-secondary btn-sm" asp-action="Index">← Back</a></div>
        <div class="page-header"><div class="page-header-info"><h1>👤 Add Participant</h1></div></div>
        <form asp-action="Create" method="post">
            <div class="card">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                    <div class="form-group">
                        <label class="form-label" asp-for="Name">Full Name *</label>
                        <input asp-for="Name" class="form-control" placeholder="e.g., Alice Johnson" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="form-label" asp-for="Email">Email *</label>
                        <input asp-for="Email" class="form-control" type="email" placeholder="alice@example.com" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label" asp-for="Phone">Phone</label>
                            <input asp-for="Phone" class="form-control" placeholder="555-0000" />
                        </div>
                        <div class="form-group">
                            <label class="form-label" asp-for="Organization">Organization</label>
                            <input asp-for="Organization" class="form-control" placeholder="Company or group" />
                        </div>
                    </div>
                    <div style="display:flex; gap:12px; justify-content:flex-end;">
                        <a class="btn btn-secondary" asp-action="Index">Cancel</a>
                        <button type="submit" class="btn btn-primary">✅ Add Participant</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
